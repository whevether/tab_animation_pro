name: tab_animation_pro
description: A Flutter tab bar with regular/irregular shapes, layered animations, configurable 3D effects, and externally supplied icons/badges. Compatible with all Flutter platforms.
version: 0.1.0
homepage: https://github.com/whevether/tab_animation_pro
repository: https://github.com/whevether/tab_animation_pro

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
