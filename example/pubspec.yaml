name: tab_animation_pro_example
description: Demo app for tab_animation_pro shapes, animations, 3D switch, and external media.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.1
  tab_animation_pro:
    path: ../
  lottie: ^3.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
